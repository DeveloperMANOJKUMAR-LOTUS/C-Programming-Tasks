#include <stdio.h>
struct info {
    int distance1;
    int distance2;
    
}s1,s2;
void main(){
    int sum;
    printf("Enter the distance1:");
    scanf("%d", &s1.distance1);
    printf("Enter the name:");
    scanf("%d", &s1.distance2);
    printf("Enter the mark:");
    sum = s1.distance1 + s1.distance2;
    printf("Sum %d",sum);
    
    
}
